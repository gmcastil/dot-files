if [ -d "${HOME}/.local/bin" ]; then
  PATH="${HOME}/.local/bin"
elif [ -d "${HOME}/bin" ]; then
  PATH="${HOME}/bin"
fi
PATH="${PATH}":${HOME}/bin:/bin:/usr/bin:/usr/local/bin:/snap/bin
EDITOR="vim"

export PATH
export EDITOR
